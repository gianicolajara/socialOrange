//"idle" | "pending" | "succeeded" | "failed"
export enum loadingState {
  "IDLE" = "idle",
  "PENDING" = "pending",
  "SUCCEEDED" = "succeeded",
  "FAILED" = "failed",
}

export enum modeFormLogin {
  "REGISTER" = "register",
  "LOGIN" = "login",
}
